# installation steps

`git clone --recurse-submodules [git link]`

If you've already cloned, use:

`git submodule update --remote --force`

Use Debian for WSL to compile from Windows.

You will need the following packages.

`sudo apt-get install clang make gcc-mingw-w64-x86-64 git`
